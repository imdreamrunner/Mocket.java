apply plugin:'application'

sourceCompatibility = 1.7
version = '1.0'
mainClassName = "space.dreamrunner.chat_server.ChatServer"

repositories {
    mavenCentral()
}

run {
    args = ["5200"]
    standardInput = System.in
}

dependencies {
    compile project(":mocket")
}