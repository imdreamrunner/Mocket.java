apply plugin:'application'

sourceCompatibility = 1.7
version = '1.0'
mainClassName = "space.dreamrunner.chat_client.ChatClient"

repositories {
    mavenCentral()
}

run {
    args = ["127.0.0.1", "5200"]
    standardInput = System.in
}

dependencies {
    compile project(":mocket")
}