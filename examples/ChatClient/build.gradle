apply plugin:'application'

sourceCompatibility = 1.7
version = '1.0'
mainClassName = "io.github.imdreamrunner.chat_client.ChatClient"

repositories {
    mavenCentral()
}

run {
    standardInput = System.in
}

dependencies {
    compile project(":mocket")
}